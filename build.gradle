apply plugin: 'cpp'

model {
    components {
        chroma(NativeLibrarySpec) {
            binaries.all {
                if (toolChain in Gcc) {
                    cppCompiler.args "-O3", "-march=native", "-Wa,-q"
                }
            }
        }
    }
}