project(":chroma-cpp") {
    apply plugin: 'cpp'

    libraries {
        chroma {
            binaries.all {
                if (toolChain in Gcc) {
                    cppCompiler.args "-O3", "-march=native", "-Wa,-q"
                }
            }
        }
    }

}